{
  "private": true,
  "workspaces": [
    "apps/web",
    "sdks/js"
  ],
  "scripts": {
    "lint": "npm run lint -w apps/web",
    "test": "npm run build -w sdks/js && npm run test -w sdks/js && npm run test -w apps/web",
    "build": "npm run build -w sdks/js"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@typescript-eslint/parser": "^6.7.0",
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "vitest": "^1.6.1"
  }
}

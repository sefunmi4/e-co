# E-CO Desktop Shell

This directory will host the Tauri-based desktop shell that wraps the web experience, exposes system integrations, and brokers SymbolCast input from the native daemon. The initial scaffolding lives here as documentation while the Rust + Tauri project is prepared in a follow-up sprint.

import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <div className="p-4">EtherOS Web UI</div>;

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
